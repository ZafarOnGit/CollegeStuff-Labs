
total = 0.0
x = int(input("Enter the total number of months"))
for i in range(1,x+1,1):
    price  = float(input("Enter the amount saved in month " + str(i) +": " ))
    total += price;
print("The total amount saved is:", total)
print("The average amount saved every month is:", total/i)